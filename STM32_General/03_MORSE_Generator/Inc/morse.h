#ifndef MORSE_H_
#define MORSE_H_

// Includes
#include "main.h"
#include "usart.h"

// Defines

// Function declarations (public)
void morse_init(uint16_t ms);
int morse_run(void);

#endif /* MORSE_H_ */
